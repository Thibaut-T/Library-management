export type PlainAuthorModel = {
    id: string;
    firstName: string;
    lastName: string;
    photoUrl: string;
  };

  export type authorToAdd = {
    firstName: string;
    lastName: string;
  };